Test zone group forwarding expansion, intra-group forwarding, Cartesian product, and comment formatting.

-- Testcase --
{%
	include("./root/usr/share/firewall4/main.uc", {
		getenv: function(varname) {
			switch (varname) {
			case 'ACTION':
				return 'print';
			}
		}
	})
%}
-- End --

-- File uci/helpers.json --
{}
-- End --

-- File uci/firewall.json --
{
	"zone": [
		{
			"name": "lan",
			"forward": "DROP",
			"device": [ "eth0" ],
			"auto_helper": 0
		},
		{
			"name": "dmz",
			"forward": "DROP",
			"device": [ "eth1" ],
			"auto_helper": 0
		},
		{
			"name": "wan",
			"forward": "REJECT",
			"device": [ "eth2" ],
			"auto_helper": 0
		},
		{
			"name": "wan6",
			"forward": "REJECT",
			"device": [ "eth3" ],
			"auto_helper": 0
		}
	],

	"zone_group": [
		{
			"name": "trusted",
			"zone": [ "lan", "dmz" ]
		},
		{
			"name": "wans",
			"zone": [ "wan", "wan6" ]
		},
		{
			"name": "empty_group",
			"zone": [ "nonexistent" ]
		}
	],

	"forwarding": [
		{
			"name": "mesh",
			"src": "trusted",
			"dest": "trusted"
		},
		{
			"src": "trusted",
			"dest": "wan"
		},
		{
			"name": "wan_out",
			"src": "lan",
			"dest": "wans"
		},
		{
			"src": "trusted",
			"dest": "wans"
		},
		{
			"name": "multi_test",
			"src": [ "dmz", "lan" ],
			"dest": "wan"
		},
		{
			"src": "empty_group",
			"dest": "wan"
		},
		{
			"name": "group_to_concrete",
			"src": "trusted",
			"dest": "lan"
		}
	]
}
-- End --

-- Expect stderr --
[!] Section @zone_group[2] (empty_group) references unknown zone 'nonexistent'
[!] Section @forwarding[5] references empty zone group 'empty_group', skipping
-- End --

-- Expect stdout --
table inet fw4
flush table inet fw4

table inet fw4 {
	#
	# Defines
	#

	define lan_devices = { "eth0" }
	define lan_subnets = {  }

	define dmz_devices = { "eth1" }
	define dmz_subnets = {  }

	define wan_devices = { "eth2" }
	define wan_subnets = {  }

	define wan6_devices = { "eth3" }
	define wan6_subnets = {  }


	#
	# User includes
	#

	include "/etc/nftables.d/*.nft"


	#
	# Filter rules
	#

	chain input {
		type filter hook input priority filter; policy drop;

		iif "lo" accept comment "!fw4: Accept traffic from loopback"

		ct state vmap { established : accept, related : accept } comment "!fw4: Handle inbound flows"
		iifname "eth0" jump input_lan comment "!fw4: Handle lan IPv4/IPv6 input traffic"
		iifname "eth1" jump input_dmz comment "!fw4: Handle dmz IPv4/IPv6 input traffic"
		iifname "eth2" jump input_wan comment "!fw4: Handle wan IPv4/IPv6 input traffic"
		iifname "eth3" jump input_wan6 comment "!fw4: Handle wan6 IPv4/IPv6 input traffic"
	}

	chain forward {
		type filter hook forward priority filter; policy drop;

		ct state vmap { established : accept, related : accept } comment "!fw4: Handle forwarded flows"
		iifname "eth0" jump forward_lan comment "!fw4: Handle lan IPv4/IPv6 forward traffic"
		iifname "eth1" jump forward_dmz comment "!fw4: Handle dmz IPv4/IPv6 forward traffic"
		iifname "eth2" jump forward_wan comment "!fw4: Handle wan IPv4/IPv6 forward traffic"
		iifname "eth3" jump forward_wan6 comment "!fw4: Handle wan6 IPv4/IPv6 forward traffic"
	}

	chain output {
		type filter hook output priority filter; policy drop;

		oif "lo" accept comment "!fw4: Accept traffic towards loopback"

		ct state vmap { established : accept, related : accept } comment "!fw4: Handle outbound flows"
		oifname "eth0" jump output_lan comment "!fw4: Handle lan IPv4/IPv6 output traffic"
		oifname "eth1" jump output_dmz comment "!fw4: Handle dmz IPv4/IPv6 output traffic"
		oifname "eth2" jump output_wan comment "!fw4: Handle wan IPv4/IPv6 output traffic"
		oifname "eth3" jump output_wan6 comment "!fw4: Handle wan6 IPv4/IPv6 output traffic"
	}

	chain prerouting {
		type filter hook prerouting priority filter; policy accept;
	}

	chain handle_reject {
		meta l4proto tcp reject with tcp reset comment "!fw4: Reject TCP traffic"
		reject with icmpx type port-unreachable comment "!fw4: Reject any other traffic"
	}

	chain input_lan {
		jump drop_from_lan
	}

	chain output_lan {
		jump drop_to_lan
	}

	chain forward_lan {
		jump accept_to_dmz comment "!fw4: mesh [lan -> dmz] (trusted)"
		jump accept_to_wan comment "!fw4: Accept lan to wan forwarding (trusted)"
		jump accept_to_wan comment "!fw4: wan_out [lan -> wan] (wans)"
		jump accept_to_wan6 comment "!fw4: wan_out [lan -> wan6] (wans)"
		jump accept_to_wan comment "!fw4: Accept lan to wan forwarding (trusted -> wans)"
		jump accept_to_wan6 comment "!fw4: Accept lan to wan6 forwarding (trusted -> wans)"
		jump accept_to_wan comment "!fw4: multi_test [lan -> wan]"
		jump drop_to_lan
	}

	chain accept_to_lan {
		oifname "eth0" counter accept comment "!fw4: accept lan IPv4/IPv6 traffic"
	}

	chain drop_from_lan {
		iifname "eth0" counter drop comment "!fw4: drop lan IPv4/IPv6 traffic"
	}

	chain drop_to_lan {
		oifname "eth0" counter drop comment "!fw4: drop lan IPv4/IPv6 traffic"
	}

	chain input_dmz {
		jump drop_from_dmz
	}

	chain output_dmz {
		jump drop_to_dmz
	}

	chain forward_dmz {
		jump accept_to_lan comment "!fw4: mesh [dmz -> lan] (trusted)"
		jump accept_to_wan comment "!fw4: Accept dmz to wan forwarding (trusted)"
		jump accept_to_wan comment "!fw4: Accept dmz to wan forwarding (trusted -> wans)"
		jump accept_to_wan6 comment "!fw4: Accept dmz to wan6 forwarding (trusted -> wans)"
		jump accept_to_wan comment "!fw4: multi_test [dmz -> wan]"
		jump accept_to_lan comment "!fw4: group_to_concrete [dmz -> lan] (trusted)"
		jump drop_to_dmz
	}

	chain accept_to_dmz {
		oifname "eth1" counter accept comment "!fw4: accept dmz IPv4/IPv6 traffic"
	}

	chain drop_from_dmz {
		iifname "eth1" counter drop comment "!fw4: drop dmz IPv4/IPv6 traffic"
	}

	chain drop_to_dmz {
		oifname "eth1" counter drop comment "!fw4: drop dmz IPv4/IPv6 traffic"
	}

	chain input_wan {
		jump drop_from_wan
	}

	chain output_wan {
		jump drop_to_wan
	}

	chain forward_wan {
		jump reject_to_wan
	}

	chain accept_to_wan {
		oifname "eth2" counter accept comment "!fw4: accept wan IPv4/IPv6 traffic"
	}

	chain reject_to_wan {
		oifname "eth2" counter jump handle_reject comment "!fw4: reject wan IPv4/IPv6 traffic"
	}

	chain drop_from_wan {
		iifname "eth2" counter drop comment "!fw4: drop wan IPv4/IPv6 traffic"
	}

	chain drop_to_wan {
		oifname "eth2" counter drop comment "!fw4: drop wan IPv4/IPv6 traffic"
	}

	chain input_wan6 {
		jump drop_from_wan6
	}

	chain output_wan6 {
		jump drop_to_wan6
	}

	chain forward_wan6 {
		jump reject_to_wan6
	}

	chain accept_to_wan6 {
		oifname "eth3" counter accept comment "!fw4: accept wan6 IPv4/IPv6 traffic"
	}

	chain reject_to_wan6 {
		oifname "eth3" counter jump handle_reject comment "!fw4: reject wan6 IPv4/IPv6 traffic"
	}

	chain drop_from_wan6 {
		iifname "eth3" counter drop comment "!fw4: drop wan6 IPv4/IPv6 traffic"
	}

	chain drop_to_wan6 {
		oifname "eth3" counter drop comment "!fw4: drop wan6 IPv4/IPv6 traffic"
	}


	#
	# NAT rules
	#

	chain dstnat {
		type nat hook prerouting priority dstnat; policy accept;
	}

	chain srcnat {
		type nat hook postrouting priority srcnat; policy accept;
	}


	#
	# Raw rules (notrack)
	#

	chain raw_prerouting {
		type filter hook prerouting priority raw; policy accept;
	}

	chain raw_output {
		type filter hook output priority raw; policy accept;
	}


	#
	# Mangle rules
	#

	chain mangle_prerouting {
		type filter hook prerouting priority mangle; policy accept;
	}

	chain mangle_postrouting {
		type filter hook postrouting priority mangle; policy accept;
	}

	chain mangle_input {
		type filter hook input priority mangle; policy accept;
	}

	chain mangle_output {
		type route hook output priority mangle; policy accept;
	}

	chain mangle_forward {
		type filter hook forward priority mangle; policy accept;
	}
}
-- End --
